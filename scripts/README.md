# Scripts

Keep repeatable project automation here. Scripts should be non-interactive where
practical, fail clearly, avoid hidden dependencies, and be exposed through the
project's eventual task runner or package manager.
